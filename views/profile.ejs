<% if(picture) { %>
    <img src="<%=picture %>" width="200" height="200">
    </p>
    <% } %>

    <% if(name) { %>
        <p> Name: <%=name %> </p>
       <% } %>

    <% if(email) { %>
        <p> Email: <%=email %>
        </p>
        <% } %>